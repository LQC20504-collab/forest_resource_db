<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.hznu.forest.mapper.UserMapper">

    <select id="findByUsername" resultType="User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user(username, password, role, real_name, phone)
        VALUES(#{username}, #{password}, #{role}, #{realName}, #{phone})
    </insert>

    <select id="findById" resultType="User">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <update id="updateUser" parameterType="User">
        UPDATE user
        SET real_name = #{realName},
            phone = #{phone}
        WHERE user_id = #{userId}
    </update>

    <update id="updatePassword" parameterType="User">
        UPDATE user
        SET password = #{password}
        WHERE user_id = #{userId}
    </update>

</mapper>
